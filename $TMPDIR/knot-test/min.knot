type Item = {name: Text, price: Int}

*items : [Item]

main = do
  replace *items = [{name: "A", price: 1}]
  let kept = (do
    i <- *items
    where i.price > 0
    yield i)
  *items = kept
  yield {}
